# v1.0.0 dependency review

Reviewed on 2026-08-24 against the open Dependabot queue. No pull-request commit was merged or cherry-picked.

| PR | Scope | Decision | Evidence |
|---|---|---|---|
| #47 | CodeQL action 4.37.7 to 4.37.8 | Applied independently | SHA pins were reviewed and updated in `security.yml`; the final exact-SHA Security workflow remains authoritative. |
| #48 | Ten compatible frontend updates | Deferred | The grouped PR has failed repository, Ubuntu backend, container and release-readiness checks. The current lock remains reproducible for the v1 RC. |
| #45 | Sixteen compatible backend updates | Deferred | The grouped PR is stale and has failed dependency, backend, container and frontend gates. Re-evaluate after v1 without mixing dependency churn into the RC. |
| #37, #39–#42 | Major updates | Deferred | Major runtime/tooling changes require isolated migration and compatibility work after v1. |

GitHub Dependabot-alert and code-scanning alert endpoints returned HTTP 404 for the available token. This is an access limitation, not evidence of zero alerts. Security workflow, SBOM and image scans remain required release gates.

# Matriz de fechamento da auditoria v0.8.6

Data do fechamento técnico: **29 de julho de 2026**. Baseline auditado: `c7aeaaf`. Esta matriz usa
somente os estados permitidos pelo pedido de implementação. `fixed` significa que a causa reproduzida
foi tratada e possui evidência no repositório; não significa validação clínica, regulatória ou
institucional. `partially_fixed` e os estados `deferred_*` mantêm o resíduo explícito.

## P0

| ID | Estado | Causa confirmada | Arquivos implementados | Testes | Commit | Evidência | Risco residual ou motivo do deferimento |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P0-01 | fixed | Motores produziam resultados independentes e o status persistido vinha apenas do `RiskEngine`. | `domain/clinical_decision.py`; `services/clinical_decision_orchestrator.py`; rotas `cds.py` e `prescriptions.py` | `test_clinical_decision_orchestrator.py`; `test_prescription_api.py` | `a4ed84e` | Envelope único, invariantes de precedência e achado `CRITICAL`/hard block impedem resultado favorável. | Rulesets demo ainda exigem validação clínica externa. |
| P0-02 | fixed | `dose_mg` era comparada com regras de unidades/dimensões incompatíveis. | `domain/dose.py`; `domain/prescription.py`; `services/dose_intelligence.py` | `test_dose_dimensions_v086.py`, inclusive propriedade de round-trip | `a4ed84e`; `75d3988` | Massa possui conversão explícita; taxa, contínua, procedimento e diária não são multiplicadas indistintamente; dimensão incompatível causa abstention. | Catálogo de unidades/rulesets permanece demonstrativo. |
| P0-03 | fixed | Fórmulas liam sexo inexistente e caíam silenciosamente no ramo feminino. | `domain/patient.py`; `services/dose_intelligence.py` | `test_dose_dimensions_v086.py::test_sex_dependent_formula_never_assumes_a_default` | `a4ed84e` | Campo opcional explícito `sex_for_dosing_calculation`; ausência produz `insufficient_data`. | Governança clínica desse dado depende da instituição. |
| P0-04 | fixed | Ausência de regra, fonte ou contexto podia continuar com sinal favorável. | `domain/clinical_decision.py`; `services/clinical_decision_orchestrator.py`; `services/source_policy.py` | casos `no rule`, `missing context` e `expired source` em `test_clinical_decision_orchestrator.py` | `a4ed84e` | `coverage_status` é obrigatório; cobertura insuficiente/expirada não pode ser favorável. | Cobertura material do catálogo é incompleta e demo. |
| P0-05 | fixed | O cliente fornecia regras/limites e o endpoint inventava peso, dose, via e máximo. | `api/routes/cds.py`; `schemas/cds_schema.py`; `services/clinical_decision_orchestrator.py` | `test_prescription_api.py`; `test_clinical_decision_orchestrator.py` | `a4ed84e` | Paciente, medicamento, ruleset e fontes são resolvidos no servidor; desconhecido ou ambíguo resulta em abstention. | Novos endpoints devem preservar resolução server-side. |
| P0-06 | fixed | O relatório histórico reconstruía evidência a partir dos cadastros atuais. | `services/clinical_snapshot.py`; `services/canonical_json.py`; `reports/evidence_bundle.py`; `reports/service.py` | `test_snapshot_and_uow_v086.py` | `a4ed84e` | Snapshot imutável e hash `sha256-canonical-json-v1`; relatório de prescrição lê somente snapshot e valida hash. | Não há assinatura externa, timestamp confiável ou WORM. |
| P0-07 | fixed | Autorização global por papel permitia BOLA/IDOR entre instituições/pacientes. | `services/object_authorization.py`; repositories e rotas de pacientes, relatórios, exports e counseling; campos `institution_id` | `test_object_authorization_v086.py` | `6d874e8` | Escopo de instituição/grant no backend e respostas negativas por objeto. | Modelo de organização/grants é demonstrativo; revisão externa endpoint a endpoint ainda é recomendada. |
| P0-08 | fixed | Segredo demo, SQLite, auto-seed, CORS local e criação automática eram aceitos fora de local. | `core/config.py`; `database/session.py`; `main.py`; `.env.example` | cenários de startup inseguro em `test_security_controls_v086.py` | `6d874e8` | Startup não local falha com defaults inseguros; health público é mínimo e readiness detalhada é protegida. | TLS, secret manager e políticas de deployment pertencem à infraestrutura. |
| P0-09 | fixed | Repositórios faziam commits intermediários durante uma decisão clínica. | `api/routes/prescriptions.py`; repositories; `services/clinical_snapshot.py` | rollback e idempotência em `test_snapshot_and_uow_v086.py` | `a4ed84e` | Decisão, eventos e snapshot usam flushes e um commit no limite da unidade de trabalho; falha reverte o conjunto. | Fluxos distribuídos futuros exigiriam outbox/coordenação. |
| P0-10 | deferred_external_validation | Não existe validação clínica formal, QMS, clinical safety officer nem avaliação regulatória independente. | `README.md`; `docs/security/clinical-safety-hazard-log.md`; `docs/security/threat-model.md`; `docs/benchmark/external-primary-sources.md` | Não aplicável a teste automatizado; revisão de claims e gates documentais | `18e9b1d`; `v0.8.6^{commit}` | Claims são limitados a demo educacional e o hazard log impede inferir liberação clínica. | Bloqueio externo: especialistas, instituição, usabilidade, gestão de risco, assessoramento jurídico/regulatório e eventual enquadramento Anvisa. |

## P1

| ID | Estado | Causa confirmada | Arquivos implementados | Testes | Commit | Evidência | Risco residual ou motivo do deferimento |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P1-01 | fixed | `v8.6.0` havia sido propagada como versão atual. | `VERSION`; manifests; UI; scripts; docs e assets correntes | `scripts/check_version_consistency.py`; `test_product_readiness_v081.py` | `29408f5` | Uma fonte canônica informa `0.8.6`; a release histórica incorreta permanece apenas como histórico advertido. | Nenhum para a numeração atual. |
| P1-02 | fixed | Versão da aplicação e `policy_version` eram tratadas como o mesmo ciclo. | schemas/model de medicamento; `prescribing_policy.py`; `clinical_decision_orchestrator.py` | `test_clinical_decision_orchestrator.py`; `test_clinical_safety_v06.py` | `a4ed84e`; `e49e254` | Policy é um identificador próprio no ruleset/snapshot e não é derivada de `VERSION`. | Policies demo ainda não foram aprovadas por governança clínica. |
| P1-03 | partially_fixed | `RiskEngine` concentrava agregação e regras em arquivo extenso. | `clinical_decision_orchestrator.py`; `clinical_decision.py`; `controlled_vocabulary.py` | `test_clinical_decision_orchestrator.py`; `test_risk_engine.py` | `a4ed84e`; `e49e254` | Agregação, cobertura e precedência saíram do engine; comportamento legado continua coberto. | O engine ainda é grande e a extração de cada regra para registry próprio não foi concluída para evitar reescrever regra clínica sem validação. |
| P1-04 | partially_fixed | Serviço de protocolos acoplava catálogo, execução, persistência, IA e apresentação. | `emergency_protocol_service.py`; rotas/schemas de protocolo; relatórios | `test_emergency_protocols_v082.py` | `6d874e8` | IA continua apenas explicativa, execução auditada e supressões globais não mascaram lint. | Serviço permanece grande; decomposição integral precisa preservar contratos e validação dos protocolos. |
| P1-05 | partially_fixed | `frontend/src/services/api.ts` centraliza muitos domínios. | `frontend/src/services/api.ts`; `AuthContext.tsx`; contratos de sessão | Vitest; `e2e/critical-flows.spec.ts`; TypeScript estrito | `6221fb6` | Sessão/cookie e tratamento de autenticação foram endurecidos sem quebrar consumidores. | Cliente ainda é monolítico; modularização completa foi deferida para não ampliar regressão de UI na release de segurança. |
| P1-06 | partially_fixed | Normalizadores tinham semânticas distintas. | `services/terminology.py`; `controlled_vocabulary.py`; checkers de alergia/interação | `test_data_integrity_v086.py`; `test_allergy_checker.py`; `test_interaction_checker.py` | `e49e254` | Matching clínico confirmado passa por terminologia/código/alias explícito. | Adapters normalizadores legados ainda existem para compatibilidade não decisória. |
| P1-07 | partially_fixed | `MedicationModel` monolítico coexiste com catálogo normalizado. | models/schemas de catálogo; `medication_catalog.py`; repositories | `test_medication_catalog_v05.py`; `test_data_integrity_v086.py` | `e49e254` | Consumidores críticos usam identidade/aliases/rulesets versionados e matching canônico. | Migração completa do modelo legado e de todos os consumidores requer migration de dados governada. |
| P1-08 | fixed | Cinco models/serviço não possuíam consumidores reais. | `database/models.py`; remoção de `services/medication_exposure.py` e referências | suíte backend; migration check | `e49e254` | Símbolos mortos e tabelas correspondentes não entram na migration baseline atual. | Reintrodução futura exige caso de uso e migration explícitos. |
| P1-09 | partially_fixed | Listagens retornavam coleções sem paginação. | rotas/repositories de pacientes, medicamentos e auditoria; export iterator | `test_data_integrity_v086.py`; `test_hardening_v085.py` | `daa872a`; `e49e254` | Pacientes/medicamentos têm paginação server-side; auditoria/export percorre páginas e declara limite. | Nem toda coleção secundária antiga foi migrada para um envelope paginado uniforme. |
| P1-10 | partially_fixed | Metadados eram carregados item a item. | `patient_repository.py`; rotas de pacientes; `catalog_repository.py` | `test_data_integrity_v086.py`; `test_medication_catalog_v05.py` | `e49e254` | Identificadores relacionados são buscados em lote nos fluxos prioritários. | Alguns detalhes do catálogo ainda podem executar consultas por item; falta benchmark PostgreSQL com volume real. |
| P1-11 | fixed | Substring em alergias/interações causava falsos positivos e negativos. | `services/terminology.py`; `allergy_checker.py`; `interaction_checker.py` | testes de colisão/alias em `test_data_integrity_v086.py`, allergy e interaction | `e49e254` | Match confirmado exige código, termo exato normalizado ou alias cadastrado; fuzzy não decide. | Qualidade dos códigos/aliases depende de curadoria. |
| P1-12 | fixed | `age` e `birth_date` podiam divergir. | `domain/patient.py`; schemas/rotas de paciente | `test_data_integrity_v086.py` | `e49e254` | `birth_date` é autoritativa, idade é calculada na data clínica e divergência recebida é rejeitada. | Migração de registros externos reais exigiria reconciliação. |
| P1-13 | partially_fixed | Condições, alergias e medicamentos atuais ainda aceitavam strings sem proveniência completa. | schemas/modelos de histórico; terminology adapters | `test_counseling_functional_reconciliation_v07.py`; `test_data_integrity_v086.py` | `e49e254` | Identidade canônica foi adicionada aos caminhos de decisão prioritários. | Persistem campos legados string; recursos clínicos totalmente estruturados e migrados ficam para evolução posterior. |
| P1-14 | fixed | Admin era tratado como prescritor. | `services/prescribing_policy.py` | `test_decision_override_v086.py`; testes de policy | `8e9072d` | `PRESCRIBING_ROLES` contém apenas o papel clínico autorizado; administração não substitui autoria clínica. | Matriz profissional demonstrativa não valida conselho/licença real. |
| P1-15 | fixed | Override e segunda revisão existiam apenas como política declarada. | `DecisionOverrideModel`; `decision_override_service.py`; schemas e endpoints | `test_decision_override_v086.py` | `8e9072d` | Justificativa, autor, segundo revisor independente e auditoria são persistidos; crítico/hard block não admite override. | Policy institucional real e coassinatura qualificada são externas. |
| P1-16 | fixed | URL livre de provider de IA permitia SSRF. | `services/safe_url.py`; `ai_settings.py`; config | casos HTTPS, allowlist e IP privado em `test_security_controls_v086.py` | `6d874e8` | HTTPS, host allowlisted e resolução apenas para IP público; redirect não é seguido; Ollama local é exceção local. | Firewall de egress e validação em cada hop pertencem ao deployment. |
| P1-17 | fixed | Breaker e credenciais eram somente em memória. | `AIProviderRuntimeStateModel`; `ai_settings.py`; migration | `test_ai_settings_quality_v071.py`; `test_security_controls_v086.py`; migration test | `8e9072d` | Estado do breaker é compartilhado no banco; credencial é criptografada; memória é aceita só local e rejeitada em produção. | Secret manager externo e rotação automatizada não fazem parte do repositório. |
| P1-18 | fixed | Frontend enviava fatos/status para a explicação clínica. | endpoint/schema de explain; `ai_explainer.py`; frontend API | `test_ai_explainer.py`; `test_security_controls_v086.py` | `6d874e8`; `6221fb6` | Cliente envia `audit_id`; backend autoriza objeto, carrega snapshot e só então compõe explicação. | Explicação externa continua opcional e não validada clinicamente. |
| P1-19 | fixed | Minimização externa serializava objeto amplo. | `ai_explainer.py`; `ai_settings.py`; audit redaction | testes de payload/segredo em `test_ai_explainer.py` e `test_security_controls_v086.py` | `6d874e8` | Allowlist exclui CPF, CNS, contato, endereço, e-mail e identificador real. | DLP independente e contrato do provider são validações externas. |
| P1-20 | partially_fixed | Retriever relia Markdown por requisição e usava interseção lexical simples. | `knowledge/retriever.py`; `rag_service.py`; documentação RAG | `test_knowledge_index_v086.py`; caso de fonte expirada no orquestrador | `e49e254`; `18e9b1d` | Índice imutável por processo, chunks, IDs, hashes, deduplicação, validade e filtro heurístico de injection. | Recall, precision, groundedness, poisoning e drift não passaram por avaliação formal. |
| P1-21 | fixed | Hash concatenava texto com `repr(payload)`. | `services/canonical_json.py`; `clinical_snapshot.py`; reports | `test_snapshot_and_uow_v086.py` | `a4ed84e` | Serialização canônica e algoritmo versionado; mutação ou hash divergente bloqueia relatório. | Assinatura/timestamp externos ausentes. |
| P1-22 | partially_fixed | Auditoria copiava nome/e-mail e aceitava detalhes livres amplos. | `audit_service.py`; `reports/audit.py`; schemas/repository | testes de redaction em `test_security_controls_v086.py`; relatórios de auditoria | `6d874e8`; `daa872a` | Novos eventos minimizam ator e removem chaves sensíveis recursivamente. | Tabelas históricas, retenção, tokenização institucional e descarte não foram migrados integralmente. |
| P1-23 | partially_fixed | Múltiplas trilhas especializadas dificultavam busca consistente. | `AuditEventModel`; `audit_repository.py`; referências de eventos | `test_reports_exports_audit_v080.py`; `test_data_integrity_v086.py` | `daa872a` | Export/report usa projeção canônica e manifesto comum. | Tabelas especializadas permanecem por compatibilidade; consolidação física exige migration/retention. |
| P1-24 | deferred_with_blocker | Filtros antigos ainda dependem de JSON convertido em texto. | Análise em `audit_repository.py`; migration PostgreSQL atual | CI PostgreSQL e `test_migrations_v086.py` demonstram schema atual, não a otimização | `b927c03`; `daa872a` | Filtros principais usam colunas quando disponíveis; não foi inventado índice sem perfil de consultas. | Bloqueio: definir projeções/JSONB indexes a partir de workload PostgreSQL, plano de migração e benchmark real. |
| P1-25 | fixed | Export de auditoria cortava 500 eventos sem declarar incompletude. | `audit_repository.py`; `evidence_bundle.py`; exporters PDF/JSON/CSV | `test_data_integrity_v086.py::test_audit_bulk_read_declares_truncation_manifest` | `daa872a` | Iteração paginada até teto explícito de 10.000 e manifesto `total_available`, `returned`, `truncated`, `limit`. | Teto é deliberado; datasets maiores exigem job/streaming assíncrono. |
| P1-26 | partially_fixed | JWT de browser era persistido em `localStorage`. | `api/routes/auth.py`; `AuthContext.tsx`; cliente API | cookie/logout em `test_security_controls_v086.py`; E2E | `6221fb6`; `6d874e8` | Browser usa cookie HttpOnly, SameSite e `withCredentials`; não grava token. | Resposta bearer ainda existe para clientes de API legados; OIDC/BFF e revogação central não foram implementados. |
| P1-27 | fixed | Login não tinha contenção de tentativa nem MFA. | `auth_throttle.py`; auth route; user model/schema | `test_security_controls_v086.py`; `test_auth_api.py` | `6d874e8` | Janela/lockout persistentes, identificador hasheado, auditoria e TOTP opcional. | Rate limit distribuído de borda e recuperação institucional de MFA são externos. |
| P1-28 | fixed | Health público expunha ambiente/provider. | `main.py`; rota protegida de settings/readiness | `test_security_controls_v086.py` | `6d874e8` | Público retorna apenas `{"status":"ok"}`; detalhes exigem autenticação/perfil. | Monitoramento interno deve ser configurado pelo deployment. |
| P1-29 | fixed | Schema dependia de `create_all` e ALTERs ad hoc. | `alembic.ini`; `migrations/env.py`; migration `3978b04e2f62`; `database/session.py` | `test_migrations_v086.py` | `b927c03` | Upgrade, downgrade, upgrade e `alembic check`; ambiente não local exige head atual. | Estratégia de migration zero-downtime precisa ser definida quando houver produção real. |
| P1-30 | fixed | CI não exercitava PostgreSQL. | `.github/workflows/ci.yml` | job PostgreSQL 17 com migration round-trip | `df00123` | Service container e Alembic bloqueiam release-readiness. | Não substitui teste de carga nem topologia gerenciada. |
| P1-31 | fixed | Ferramentas de teste estavam no runtime. | `requirements.txt`; `requirements-lock.txt`; `requirements-dev.txt` | instalação limpa do lock, `pip check`, CI backend | `8f2562b` | `pytest`, `pytest-cov`, Hypothesis, Ruff e seus pacotes próprios ficam no grupo dev; import de runtime foi validado em venv limpa. | O lock deve ser regenerado a cada upgrade deliberado. |
| P1-32 | fixed | `npm run lint` era somente typecheck. | `eslint.config.js`; `package.json`; componentes ajustados | `npm run lint`; Vitest; build | `6221fb6` | ESLint real com zero warnings, React Hooks e regras locais de acessibilidade compatíveis com ESLint 10. | Auditoria assistiva manual continua necessária. |
| P1-33 | fixed | Não havia SAST, SCA, secret scan ou SBOM. | `.github/workflows/security.yml`; scripts de audit; `requirements-security.txt` | CodeQL, pip-audit, npm audit controlado, gitleaks e CycloneDX | `df00123` | Gatilhos e gates versionados; risco temporário único documentado com expiração. | Scans não substituem pentest, DAST ou revisão jurídica de licenças. |
| P1-34 | partially_fixed | Publicação direta em `main` não exige revisão independente. | `.github/CODEOWNERS`; PR template; docs de contribuição | Verificação documental | `df00123` | Owners e política de revisão estão versionados. | Branch protection/ruleset é estado externo; o próprio pedido desta release autorizou commits diretos em `main`. |
| P1-35 | fixed | Actions usavam tags mutáveis. | workflows `ci.yml` e `security.yml` | inspeção dos workflows e execução no SHA final | `df00123` | Todas as Actions de terceiros estão fixadas por SHA com versão em comentário. | SHAs ainda requerem revisão deliberada em upgrades. |
| P1-36 | fixed | Faltavam processo de vulnerabilidade, owners e atualização automatizada. | `SECURITY.md`; `.github/CODEOWNERS`; `dependabot.yml` | checks de links/texto | `df00123` | Canal privado, SLA/escopo, ownership e updates semanais documentados. | Operação efetiva depende dos recursos/contatos do repositório GitHub. |
| P1-37 | fixed | README/docs correntes funcionavam como release notes datadas. | `README.md`; `docs/README.md`; arquitetura/RAG/operações; `docs/releases/v0.8.6.md` | links, assets, texto e consistência de versão | `18e9b1d`; `v0.8.6^{commit}` | Entrada principal é version-neutral; mudanças ficam no changelog e nota versionada. | Auditorias históricas permanecem intencionalmente datadas. |
| P1-38 | fixed | `docs/audience` e `docs/audiences` coexistiam. | `docs/audiences`; links atualizados; diretório singular removido | `scripts/check_markdown_links.py` | `4d5fa41` | Um único diretório canônico e índice consolidado. | Nenhum conhecido. |
| P1-39 | partially_fixed | Assets históricos eram numerosos e duplicados. | `docs/assets/current`; `docs/assets/v0.8.6`; documentação histórica preservada | `scripts/check_assets.py` | `29408f5`; `v0.8.6^{commit}` | Conjunto corrente mínimo é referenciado pelo README; assets atuais estão separados da evidência histórica. | Histórico pesado não foi reescrito nem movido para LFS para preservar rastreabilidade e evitar operação destrutiva. |
| P1-40 | partially_fixed | E2E validava sobretudo presença/navegação. | `frontend/e2e/critical-flows.spec.ts`; testes backend clínicos e de autorização | Playwright; `test_clinical_decision_orchestrator.py`; `test_object_authorization_v086.py`; `test_security_controls_v086.py` | `6d874e8`; `6221fb6`; `75d3988` | Decisão, abstention, adulteração, acesso negativo, cookie, SSRF e rollback têm regressões automatizadas. | Playwright ainda não cobre toda a matriz de false-green/BOLA; parte crítica é coberta no backend, fonte autoritativa. |

## Resumo

- P0: 9 `fixed`; 1 `deferred_external_validation`.
- P1: 24 `fixed`; 15 `partially_fixed`; 1 `deferred_with_blocker`.
- Nenhum achado foi marcado `not_reproducible`.

Os resíduos acima são backlog explícito, não autorização para uso real. A evidência de execução final e
o conteúdo exato publicado estão no [relatório final](v0.8.6-final-quality-and-release-report.md) e na
tag anotada `v0.8.6`.

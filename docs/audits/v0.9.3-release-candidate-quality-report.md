# v0.9.3 release-candidate quality report

Status: **local RC passed; exact-SHA remote gates pending**.

Focused validation completed during implementation:

- numeric/CDP security: 14 Python tests and 2 Node tests passed;
- causal, NL→SQL and reference fixture: 18 tests passed; PostgreSQL-only tests are wired into CI;
- evidence/agent: 5 mocked/adversarial tests passed;
- frontend: ESLint, TypeScript and Vite production build passed.

## Single local RC — 2026-08-13

- repository quality: text, Markdown links, assets, version consistency, install-script policy,
  dependency audit and 811-key i18n equivalence passed;
- backend: Ruff passed; 268 tests passed and the 2 PostgreSQL-only tests skipped in SQLite;
- backend coverage: 82.44% combined and 65.61% branches, both above the independent ratchets;
- frontend: ESLint/typecheck/build passed; 89 Vitest tests passed; coverage reached 84.24% lines,
  82.50% statements, 71.04% branches and 78.78% functions without lowering thresholds;
- E2E/accessibility/visual/responsive: 19 passed, 2 project-selection skips;
- PostgreSQL 17: planner/read-only tests 2 passed; Alembic upgrade → downgrade base → upgrade head
  completed and `alembic check` reported no new operations.

Two RC findings were corrected before closure: the frontend version constant was stale, and a legacy
query-preview mock lacked the new optional planner object. Coverage then exposed missing Evidence/Agent
interaction tests; those tests were added rather than reducing the ratchet. Local E2E now accepts an
explicit `PRESCRIPTA_PYTHON` so the validated virtual environment is used on Windows.

The exact final SHA, remote runs, tag verification, release assets and checksums are added after the
remote release gates. Container/SBOM authority remains the remote exact-SHA workflows because no
Docker runtime or image definition changed in v0.9.3.

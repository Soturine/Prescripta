# v1.0.0 release-candidate quality report

Status: **local RC completed with focused remediation; exact-SHA remote gates pending**.

Prescripta v1.0.0 remains synthetic/demo-only. Deterministic services remain authoritative,
AI output remains proposal-only, and human approval remains required where documented.

## Single local RC — 2026-08-24

- repository quality passed: text, Markdown links, governed assets, version consistency,
  install-script policy, dependency audit, PT-BR/EN-US equivalence and OpenAPI compatibility;
- OpenAPI comparison from v0.10.0 to v1.0.0 reported no detected breaking change;
- backend passed Ruff and 282 tests;
- backend coverage passed the independent ratchets at 82.56% combined and 65.96% branches;
- frontend passed ESLint, strict TypeScript and the production build;
- the initial 92-test frontend coverage run exposed two stale Research fixtures: 90 tests passed
  before the run stopped; the corrected Research file then passed 7/7 in the focused rerun;
- the E2E matrix exposed insufficient contrast in Dashboard task descriptions and invalid
  definition-list nesting around linked metrics; after correction, the focused Axe scenario
  passed 1/1;
- exact-SHA CI then exposed one stale E2E selector for the decomposed Research hierarchy; the
  selector now enters `Análise` and `Plano e qualidade`, and its focused scenario passed 1/1;
- visual evidence was recaptured after the accessibility correction: 11 stable and 11
  v1.0.0-versioned assets pass hash, dimensions and size validation;
- PostgreSQL 17 migrated a clean database through the v0.10.0 baseline/head, `alembic current`
  reported `a10f0c0d1000 (head)`, and `alembic check` reported no new upgrade operations;
- npm production audit reported zero high/critical vulnerabilities.

The complete frontend coverage gate is intentionally not claimed as a local pass: its single RC
execution stopped on the two fixture failures above, and only the affected tests were rerun after
the correction. CI on the exact final SHA is the authoritative clean rerun; the local full RC was
not repeated merely to replace this evidence.

## Release gates and bounded limitations

The tag may be created only after CI, Security, Container and Qualification pass against the exact
final SHA. Release Provenance then runs from the annotated tag and publishes the verified assets.

- CVE-2026-14456 remains an explicit, expiring accepted risk for the unused QUIC server-listener
  path; ignored is not fixed, and the exception expires on 2026-09-07.
- Dependabot and code-scanning alert APIs returned HTTP 404 for the available token. No zero-alert
  claim is made; exact-SHA Security is the release authority.
- Human AI evaluation remains owner-pending and is not replaced by automated corpus results.
- OMOP remains described as CDM 5.4 partial; no DQD, Achilles/ATLAS or external compatibility claim
  is inferred from the local adapter tests.

## Delay record

The user reported repeated delays during the v0.9.1, v0.9.2, v0.9.3, v0.10.0 and v1.0.0 rounds.
For this RC, the concrete elapsed work came from the 282-test backend coverage suite, the 92-test
frontend coverage run, the E2E/accessibility matrix, two visual-evidence captures, the two focused
fixture repairs, the focused Axe repair and PostgreSQL startup/migration. Docker was initially
unavailable and the PostgreSQL step resumed immediately after the user opened Docker Desktop.
No blind polling or second full local RC was performed.
